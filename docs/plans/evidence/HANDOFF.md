# HANDOFF — security remediation 2026-09-11 (round 2)

Workhorse finished round-2 follow-ups from `docs/plans/evidence/REVIEW-ROUND-1.md`. Independent Section 8.1 review is next. This file is not a substitute for evidence blocks.

## Branch

`gol/security-remediation-2026-09-11`

Base: `main` @ `8857139af12fb033b4dd04eabd8d19b5bfc5ffc6`

Draft PR: https://github.com/hexuria/gpui-agent/pull/33 (into `main`). **Do not merge. Do not mark ready.**

## Counts

- `BASELINE_PASSED`: **194**
- Round-1 HEAD CI-package passed count: **223** (flake: `rpc_pipeline_wrong_token_fails_fast` EPIPE; R4 round 2 treats `Broken pipe` as auth-close)
- Final CI-package passed count: **232** (`cargo test -p gpui-agent -p todo-core -p gpui-agent-cli -p gpui-agent-recipe` after R15 round 2)
- Split: 91 + 30 + 8 + 9 + 64 + 16 + 13 + 1
- R11b skipped: **yes** (D5)

## Decisions in force

D1=default-deny, D2=implement protocol v2, D3=confine screenshots, D4=protocol-only registry with schema files, D5=Phase 1 selectors only.

## Commit list

`git log --oneline 8857139..HEAD` at the last task commit (`ad5b808` R15 round 2). The docs commit that filled evidence SHAs / rewrote this file follows it.

```
ad5b808 R15 (round 2): INTEGRATING test requires HMAC and confine
69ebbf9 R11 (round 2): plan_click fail-closed on duplicate ids
78df9c1 R10 (round 2): thread --schema into MCP recipe tools
d36ac7e R4 (round 2): close on bad JSON; v1-accepts-token red
f958d29 R5 (round 2): assert write_png_in is atomic, not fs::write
5b79ae8 R3 (round 2): confine screenshot paths before unavailable
abfdfd7 R1 (round 2): hide GPUI_AGENT_TOKEN values in --help
49eefab R2 (round 2): P2 table host from_env is default-deny
623080e T0 (round 2): record BLOCKED-ENV for todo/fontconfig
d3a1ca8 docs: independent review round 1 of security remediation
7f19e53 docs: record draft PR #33 in HANDOFF
a784fa0 docs: note operator override to push draft PR
951377d docs: write reviewer HANDOFF after R15
3ad6241 R15: INTEGRATING adapter checklist
0464504 R14: clippy Result size and unsafe wording
ada2a71 R13: CI generate-lockfile and cargo audit
12eed9a R12: fix protocol and README doc drift
7bbea1c R11: fail-closed duplicate ids in tree lookup
e228918 R10: protocol-only recipe registry loads app schemas
a607982 R9: Wait.timeout_ms polls hello.ready
4414843 R4: protocol v2 HMAC-SHA256 challenge-response
243ab78 R1: docs and CLI help are embed-only
a91548e R8: smoke-desktop starts daemon SoT per ADR-001
f565031 R7: mailbox hello.auth matches server token
dc6faea R5: atomic screenshot write and dash-safe argv
037eb4e R6: do not pre-delete screenshot destinations
0931363 R3: confine screenshot paths to host base dir
82a6d22 R2: default-deny host token unless GPUI_AGENT_INSECURE_NO_TOKEN
8e09eba T0: record security-remediation baseline
```

Round-2 task SHAs (reachable; `git cat-file -t` = `commit`):

| Task | SHA |
| --- | --- |
| T0 | `623080ef089ce2417489487e9947d775a2b7ca4f` |
| R2 | `49eefab057b68bfe0dbd82fd2c3ea29464c231ff` |
| R1 | `abfdfd74e733cd295e197a4a1069d47ec6d190f1` |
| R3 | `5b79ae870749b70440c208499036a5a5d4976a26` |
| R5 | `f958d2996b26d703e8dfa481eebdeabade452c0e` |
| R4 | `d36ac7ebfbfa87013807ab78d0836a226238debc` |
| R10 | `78df9c1a04521905d528d6734dbaec6309299d7a` |
| R11 | `69ebbf953ad519142ec3c6dc39df56cfffdd16e0` |
| R15 | `ad5b80833180b4bf5fe0bff9df736052b7aad8b4` |

Round-1 task SHAs are in the evidence file `Commit:` fields (rewritten in T0 round 2).

## Evidence

`docs/plans/evidence/2026-09-11-remediation-evidence.md`

Round-1 review: `docs/plans/evidence/REVIEW-ROUND-1.md` (REJECT). Round-2 blocks are tagged `(round 2)`.

## BLOCKED-ENV

**`apps/todo` / fontconfig** (T0 round 2, this Ubuntu 24.04.4 VM):

- `sudo apt-get install -y libfontconfig1-dev pkg-config` → exit 100, `E: Unable to locate package libfontconfig1-dev`
- Apt offers `libfontconfig1` runtime only (`fontconfig.pc` missing)
- `cargo check -p todo` and `cargo check -p todo --features embedded-host` → exit 101 (`yeslogic-fontconfig-sys`)

Round-2 `apps/todo` edits (macOS handoff): R3 `screenshot_this_window` confines before capture / `screenshot_unavailable`. Operator: `cargo check -p todo` and `cargo check -p todo --features embedded-host` locally.

## Deviations

Round 1 (still true):

- T0: `BASELINE_PASSED` is 194, not G's 196. `Cargo.lock` generated by cargo and left untracked (not committed).
- R8: `cargo build` split (`cli`+`todo-headless` first, then `todo`). Installed `libxkbcommon-dev`/`libxkbcommon-x11-dev` once so `cargo build -p todo` could link. GUI start was attempted (`DISPLAY` set) and skipped after `Failed to create surface`. Smoke script executed and exited 0.
- R1 (round 1): help test used `Cli::command().render_long_help()`; round 2 added a real binary spawn canary.
- R4 (round 1): `rpc_pipeline_wrong_token_fails_fast` also accepted connection-reset; round 2 adds `Broken pipe` / `os error 32`. Untokened `AgentClient` vs tokened v2 host maps a leftover challenge line to `automation token required`. `click_without_delivery_is_semantic` still parses old `"v":1` documents (serde, not live authorize). No `tokio`.
- R10 (round 1): CLI tests that validate/run `todo-crud.json` pass `--schema examples/schemas/todo.json`. `todo_registry()` kept for in-process tests.
- R11 / R15: R11b skipped (D5).
- R12: also added `page_settings_readme_role_is_page`; corrected SECURITY L6 Wait wording and the workspace-`unsafe` sentence.
- R13: pinned `cargo-audit` 0.22.2 (0.21.2 failed to parse current advisory-db CVSS 4.0). `Cargo.lock` generated locally for audit, left untracked. Did not use `--deny warnings` (5 unmaintained GPUI-stack warnings).
- R14: boxed `authorize_request` error (`Box<Response>`) rather than `#[allow]`. Also rewrote pre-existing `clippy::while_let_loop` in `mcp.rs`.

Round 2:

- Evidence `Commit:` SHAs for round-2 tasks are filled in this docs commit (not by amending task commits).
- R4: grouped named red, EPIPE, HTTP/non-JSON close, and hmac/sha2 docs in one round-2 commit. Reachable v1-accepts-token red: `docs/plans/evidence/r4-round2-red-v1-authorize-request.rs` on parent `243ab78`. Green HTTP close also treats write-side `Broken pipe` as close. v2 HMAC not weakened.
- R5: red reconstructed on parent `037eb4e` (`write_png_in` still `fs::write`). Original atomic-write stub test kept.
- R10: threaded `--schema` (did not document MCP as env-only). Recipe MCP tools also accept a `schema` arg. No live stdio MCP spawn.
- R15: strengthened existing test (count stayed 232). HMAC red reconstructed on `3ad6241` by stripping `HMAC-SHA256` from INTEGRATING.md so the original two asserts still pass. Production INTEGRATING.md not edited.

## Caps (this tree)

`cap_tests::security_caps_unchanged` ok. `MAX_LINE_BYTES = 1024 * 1024`, `MAX_CONNECTIONS = 32`, `MAX_MAILBOX_DEPTH = 128`, `MAX_RECIPE_STEPS = 256`, `IDLE_TIMEOUT = 30s`.

## Push / PR

Operator override: push `gol/security-remediation-2026-09-11` (updates **draft** PR https://github.com/hexuria/gpui-agent/pull/33). Do not merge. Do not mark ready for review. Independent Section 8.1 review of non-CONFIRMED round-1 tasks is next.
